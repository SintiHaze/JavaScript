// Defining the Variables
const country = 'Australia';    // Constant variable
const continent = 'Oceania';    // String
let population = 25000000;      // Floating point number, variable is let because it changes over time
const isIsland = true           // Boolean
let language = 'English';        // Undefined variable

console.log(country);
console.log(continent);
console.log(isIsland);
console.log(language);
console.log(population);
console.log(population / 2)     // If population of the country halved
console.log(continent + ' ' + country);